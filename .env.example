# Kleinanzeigen Credentials
KLEINANZEIGEN_EMAIL=your-email@example.com
KLEINANZEIGEN_PASSWORD=your-password

# Optional: Run in headless mode (default: true)
HEADLESS=true

# Optional: Profile to use (main, human-sim, fresh)
PROFILE=main
